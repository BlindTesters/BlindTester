// Initial functions to test.
const sum = function (a, b, c) {
  return a + b + c;
}

const diff = function (a, b, c) {
  return a - b - c;
}

const mult = function (a, b, c) {
  return a * b * c;
}

const div = function (a, b, c) {
  return a / b / c;
}

module.exports = { diff, div, mult, sum };
